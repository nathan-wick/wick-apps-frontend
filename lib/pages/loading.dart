import 'base.dart';

class LoadingPage extends BasePage {
  const LoadingPage({super.key})
    : super(
        name: 'Loading...',
        // TODO Create content
        content: const [],
      );
}
